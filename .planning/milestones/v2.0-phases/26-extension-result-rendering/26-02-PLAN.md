---
phase: 26-extension-result-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/src/types.ts
  - extension/src/SseStream.ts
autonomous: true
requirements:
  - EXT-04
  - EXT-05
  - EXT-06
  - EXT-07
  - EXT-08
  - EXT-09

must_haves:
  truths:
    - "HostToWebviewMessage union includes a 'result' variant with intent, result, has_github_token, file_written, written_path"
    - "SseStream.ts handles SSE 'result' event and forwards it as a postMessage to the webview"
    - "TypeScript compiles without errors after the type union is extended"
  artifacts:
    - path: "extension/src/types.ts"
      provides: "ResultMessage variant in HostToWebviewMessage discriminated union"
      contains: "has_github_token"
    - path: "extension/src/SseStream.ts"
      provides: "case 'result' in the SSE event switch that calls webview.postMessage"
      contains: "case 'result'"
  key_links:
    - from: "extension/src/SseStream.ts"
      to: "extension/src/webview/App.tsx"
      via: "webview.postMessage({ type: 'result', intent, result, has_github_token, file_written, written_path })"
      pattern: "postMessage.*result"
---

<objective>
Add the plumbing layer that bridges the new backend SSE `result` event to the VS Code webview. This involves two thin changes: extending the TypeScript message union in `types.ts` and adding a `case 'result'` handler to the existing switch in `SseStream.ts`.

Purpose: Without these changes the webview never receives the structured result payload — the SSE event arrives but is silently dropped. This plan gates all three panel rendering plans (Plans 03 and 04).

Output: `types.ts` with extended union, `SseStream.ts` with result case. No UI yet.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-extension-result-rendering/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HostToWebviewMessage union in types.ts</name>
  <files>extension/src/types.ts</files>
  <action>
    Read `extension/src/types.ts` in full first to understand the existing discriminated union structure.

    Add a new variant to the `HostToWebviewMessage` discriminated union (the union used for messages FROM the extension host TO the webview). The new variant:

    ```typescript
    | {
        type: 'result';
        intent: string;
        result: Record<string, unknown>;
        has_github_token?: boolean;
        file_written?: boolean;
        written_path?: string | null;
      }
    ```

    Place it at the end of the union alongside `token`, `citations`, `done`, `error`, and the other existing variants. Do NOT modify any existing variant.

    Also check if `App.tsx` defines its own local `IncomingMessage` type (a copy of the union). If `types.ts` exports a shared type and `App.tsx` imports it, one change suffices. If `App.tsx` has its own local copy, note this — it will need to be updated in Plan 03 as well. Do NOT touch `App.tsx` in this plan.
  </action>
  <verify>
    Run from extension directory: `cd /Users/mohammedhafiz/Desktop/Personal/nexus/extension && npx tsc --noEmit 2>&1 | head -30`
    Expected: Zero TypeScript errors on types.ts changes.
  </verify>
  <done>
    - `types.ts` HostToWebviewMessage union includes the `result` variant with all five fields
    - `npx tsc --noEmit` produces no new errors related to types.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Add result case to SseStream.ts switch</name>
  <files>extension/src/SseStream.ts</files>
  <action>
    Read `extension/src/SseStream.ts` in full. Locate the `switch (data['type'])` block that handles SSE event data (currently handles `token`, `citations`, `done`, `error` cases).

    Add a new case immediately before the `default` case (or after `error`):

    ```typescript
    case 'result': {
      void webview.postMessage({
        type: 'result',
        intent: data['intent'] as string,
        result: data['result'] as Record<string, unknown>,
        has_github_token: data['has_github_token'] as boolean | undefined,
        file_written: data['file_written'] as boolean | undefined,
        written_path: data['written_path'] as string | null | undefined,
      });
      break;
    }
    ```

    The `webview` variable is already in scope (it is the `Webview` passed into the stream). Use the same `void webview.postMessage(...)` pattern as the existing `citations` case.

    Do NOT change any existing case. Do NOT change the SSE event parsing (the `EventSource`/`fetch` call above the switch).
  </action>
  <verify>
    Run: `cd /Users/mohammedhafiz/Desktop/Personal/nexus/extension && npx tsc --noEmit 2>&1 | head -30`
    Expected: Zero TypeScript errors.
    Also grep: `grep -n "case 'result'" /Users/mohammedhafiz/Desktop/Personal/nexus/extension/src/SseStream.ts`
    Should return one line with the new case.
  </verify>
  <done>
    - `SseStream.ts` contains `case 'result'` in the SSE switch
    - postMessage forwards intent, result, has_github_token, file_written, written_path
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
Run: `cd /Users/mohammedhafiz/Desktop/Personal/nexus/extension && npx tsc --noEmit 2>&1 | head -20`
Pass condition: Zero TypeScript errors. The `result` case is present in SseStream.ts and the type variant is present in types.ts.
</verification>

<success_criteria>
- types.ts HostToWebviewMessage union has result variant
- SseStream.ts forwards SSE result event to webview via postMessage
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-extension-result-rendering/26-02-SUMMARY.md`
</output>
