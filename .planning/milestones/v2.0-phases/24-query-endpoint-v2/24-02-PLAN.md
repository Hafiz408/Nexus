---
phase: 24-query-endpoint-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - backend/tests/test_query_router_v2.py
autonomous: true
requirements:
  - TST-09

must_haves:
  truths:
    - "test_query_router_v2.py runs fully offline — no live LLM calls, no live database"
    - "intent_hint=debug routes to orchestrator and SSE body contains event: result with intent=debug"
    - "intent_hint=auto falls through to the V1 path — orchestrator is NOT invoked"
    - "intent_hint=None falls through to the V1 path — orchestrator is NOT invoked"
    - "Graph.invoke error is surfaced as event: error in the SSE stream"
    - "All tests pass when run alongside the V1 test suite (no fixture conflicts)"
  artifacts:
    - path: "backend/tests/test_query_router_v2.py"
      provides: "V2 endpoint test suite — 8 offline tests"
      contains: "def test_v2_debug_intent_returns_result_event"
  key_links:
    - from: "backend/tests/test_query_router_v2.py"
      to: "app.api.query_router.build_graph"
      via: "patch at consumer module (not source module)"
      pattern: "patch.*app\\.api\\.query_router\\.build_graph"
    - from: "backend/tests/test_query_router_v2.py"
      to: "app.api.query_router.get_status"
      via: "monkeypatch.setattr"
      pattern: "monkeypatch\\.setattr.*get_status"
---

<objective>
Write the V2 endpoint test suite — 8 offline tests covering all V2 routing scenarios,
sentinel handling, SSE payload structure, and error propagation. Zero live API calls.

Purpose: Satisfy TST-09 — V2 agent tests use mock LLM + mock graph with no live
external calls — applied at the endpoint integration level.

Output: backend/tests/test_query_router_v2.py with 8 tests, all passing offline.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-query-endpoint-v2/24-RESEARCH.md
@.planning/phases/24-query-endpoint-v2/24-01-SUMMARY.md

# V1 test file — exact fixture and helper pattern to replicate
@backend/tests/test_query_router.py

# Agent result types needed for mock construction
@backend/app/agent/debugger.py
@backend/app/agent/reviewer.py
@backend/app/agent/tester.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_query_router_v2.py with 8 offline V2 endpoint tests</name>
  <files>backend/tests/test_query_router_v2.py</files>
  <action>
Create backend/tests/test_query_router_v2.py. Mirror the V1 test file structure exactly:
same `client` fixture (patch init_db + init_pgvector_table), same `_read_stream` helper,
same monkeypatch approach for get_status and load_graph.

CRITICAL — patch target for build_graph:
  Patch at the consumer module: `"app.api.query_router.build_graph"`
  NOT `"app.agent.orchestrator.build_graph"` — lazy imports bind the name inside
  the function body at call time; to intercept it, patch the name in the consumer's
  namespace after the import executes (Pitfall 5 in RESEARCH.md).

Helper: `_make_mock_graph(intent, specialist_result)` — returns a MagicMock whose
  `.invoke.return_value` is a complete NexusState-shaped dict:
  ```python
  {
      "question": "test", "repo_path": "/repo", "intent_hint": intent,
      "G": None, "target_node_id": None, "selected_file": None,
      "selected_range": None, "repo_root": None,
      "intent": intent,
      "specialist_result": specialist_result,
      "critic_result": MagicMock(passed=True),
      "loop_count": 0,
  }
  ```

Also add `_make_status()` returning `IndexStatus(status="complete")`.

The 8 tests to write:

1. `test_v2_debug_intent_returns_result_event`
   — intent_hint="debug", DebugResult specialist_result
   — assert "event: result" in body, "event: done" in body, '"intent": "debug"' in body
   — mock_graph.invoke called exactly once

2. `test_v2_result_event_contains_result_key`
   — intent_hint="debug", DebugResult specialist_result
   — parse the data: line of "event: result"; assert JSON has keys "type", "intent", "result"

3. `test_v2_review_intent_routes_to_orchestrator`
   — intent_hint="review", ReviewResult specialist_result (minimal fixture)
   — assert "event: result" in body, '"intent": "review"' in body

4. `test_v2_test_intent_routes_to_orchestrator`
   — intent_hint="test", TestResult specialist_result (minimal fixture)
   — assert "event: result" in body, '"intent": "test"' in body

5. `test_v2_explain_intent_routes_to_orchestrator`
   — intent_hint="explain", _ExplainResult-like MagicMock with model_dump returning dict
   — assert "event: result" in body

6. `test_v2_auto_sentinel_uses_v1_path`
   — intent_hint="auto" → V1 path; monkeypatch explore_stream to an async gen
   — build_graph must NOT be called; body contains "event: token"
   — Use patch("app.api.query_router.build_graph") as mock_bg; assert mock_bg.call_count == 0

7. `test_v2_none_intent_hint_uses_v1_path`
   — No intent_hint field in request body (defaults to None) → V1 path
   — build_graph NOT called; body contains V1 SSE events

8. `test_v2_orchestrator_error_yields_error_event`
   — intent_hint="debug"; mock_graph.invoke raises RuntimeError("graph failed")
   — body contains "event: error", "graph failed" in body

For tests 6 and 7 (V1 path): also monkeypatch:
  `"app.api.query_router.graph_rag_retrieve"` → lambda returning ([], {})
  `"app.api.query_router.explore_stream"` → async gen yielding "tok"

For DebugResult construction use:
  ```python
  from app.agent.debugger import DebugResult, SuspectNode
  DebugResult(
      suspects=[SuspectNode(node_id="a.py::fn", file_path="/repo/a.py",
                            line_start=1, anomaly_score=0.8, reasoning="r")],
      traversal_path=["a.py::fn"], impact_radius=[], diagnosis="fn is suspect."
  )
  ```

For ReviewResult (minimal) use MagicMock with model_dump returning a plain dict:
  `mock_review = MagicMock(); mock_review.model_dump.return_value = {"findings": []}`

For TestResult (minimal) use the same MagicMock pattern.

All tests must NOT import SqliteSaver or sqlite3 — build_graph is patched so the
SqliteSaver creation inside the endpoint is never reached.

File header docstring: explain it covers TST-09, all mocked, no live calls.
  </action>
  <verify>
cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/test_query_router_v2.py -v --tb=short 2>&1 | tail -25
  </verify>
  <done>All 8 tests in test_query_router_v2.py pass. Zero failures, zero errors. No live LLM calls or database connections made during the run.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm full test suite passes (V1 + V2 together)</name>
  <files></files>
  <action>
Run the complete backend test suite to confirm:
1. All 8 original V1 tests in test_query_router.py still pass (TST-08)
2. All 8 new V2 tests in test_query_router_v2.py pass (TST-09)
3. No fixture conflicts or collection errors between the two test files

Command:
  cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/test_query_router.py tests/test_query_router_v2.py -v

If any test fails, diagnose the root cause:
- 422 errors on V1 tests → a new QueryRequest field is not Optional or has no default
- build_graph call count assertion fails → patch target is wrong (must be "app.api.query_router.build_graph")
- "event: token" missing on V2 auto/None tests → V1 path not reached; check gate condition
- Collection errors (duplicate fixture names) → rename any conflicting fixture in v2 file

Fix and re-run until all 16 tests pass green.
  </action>
  <verify>
cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/test_query_router.py tests/test_query_router_v2.py -v 2>&1 | tail -30
  </verify>
  <done>16 tests pass: 8 from test_query_router.py (V1) + 8 from test_query_router_v2.py (V2). Zero failures. Full test count reported by pytest shows no regressions in the broader suite.</done>
</task>

</tasks>

<verification>
Run the complete backend suite to confirm both requirements are satisfied:

```
cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/ -v 2>&1 | tail -30
```

Expected: 190 tests passing (182 existing + 8 new V2 endpoint tests). Zero failures.

Spot-check that no test makes live calls:
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/test_query_router_v2.py -v -s 2>&1 | grep -i "api\|openai\|live"
```
Should print nothing (no live call indicators).
</verification>

<success_criteria>
- `backend/tests/test_query_router_v2.py` exists with exactly 8 test functions
- All 8 tests pass offline (no environment variables or live services required)
- The 8 original V1 tests in test_query_router.py continue to pass (zero regressions)
- build_graph is patched at `app.api.query_router.build_graph` in every test that exercises the V2 path
- Tests 6 and 7 (auto/None sentinel) assert build_graph is NOT called
- Full suite count is 190 (182 pre-existing + 8 new)
</success_criteria>

<output>
After completion, create `.planning/phases/24-query-endpoint-v2/24-02-SUMMARY.md` using the
standard summary template. Include: test count (8 new), patch targets used, sentinel coverage
(auto + None), TST-08 and TST-09 marked complete, total passing test count.
</output>
