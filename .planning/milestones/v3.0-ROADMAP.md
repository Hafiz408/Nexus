# v3.0 Local-First Privacy — Archive (Shipped: 2026-03-25)

## Milestone Goal
Move all index storage from the server into the user's workspace — no Postgres, no Docker, zero user data stored server-side.

## Phases (27–29)
- Phase 27: wire-review-test-e2e — Wire target_node_id through extension→backend; fix reviewer/tester crashes (completed 2026-03-25)
- Phase 28: local-first-privacy — sqlite-vec replaces pgvector; workspace .nexus/graph.db; backend stateless; Postgres/Docker removed (completed 2026-03-25)
- Phase 29: extension-integration — db_path threaded through all BackendClient methods; SidebarProvider derives and sends workspace db path (completed 2026-03-25)

## Key Accomplishments
- Workspace-local graph + vector store: SQLite + sqlite-vec at .nexus/graph.db inside user's repo
- Postgres/pgvector dependency fully removed; db/database.py deleted
- Backend made stateless — compute only (parsing + LLM calls), no storage
- All BackendClient methods and query/index API endpoints accept db_path parameter
- target_node_id wired extension→backend; reviewer and tester agents no longer crash on E2E flows
