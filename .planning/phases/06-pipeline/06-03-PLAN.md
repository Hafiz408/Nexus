---
phase: 06-pipeline
plan: "03"
type: tdd
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - backend/tests/test_pipeline.py
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "run_ingestion returns status='complete' with correct nodes_indexed, edges_indexed, files_processed counts"
    - "get_status returns the IndexStatus stored by the most recent run_ingestion call for that repo_path"
    - "Incremental path calls delete_nodes_for_files with the changed_files list before re-parsing"
    - "A walk_repo or build_graph error causes run_ingestion to return status='failed' with the error message"
    - "parse_file errors within gather are caught per-file and do not abort the entire ingestion"
  artifacts:
    - path: "backend/tests/test_pipeline.py"
      provides: "Unit tests for pipeline.py with all I/O stages mocked"
      exports: []
  key_links:
    - from: "backend/tests/test_pipeline.py"
      to: "backend/app/ingestion/pipeline.py"
      via: "unittest.mock.patch on walk_repo, parse_file, build_graph, save_graph, embed_and_store, delete_nodes_for_files"
      pattern: "patch.*app\\.ingestion\\.pipeline\\."
---

<objective>
Write unit tests for pipeline.py using the TDD approach — tests describe the contract, then verify the already-implemented pipeline satisfies it.

Purpose: Tests lock in the pipeline behavior so future changes don't silently break orchestration. All I/O stages are mocked so tests run without Docker, OpenAI keys, or SQLite state.
Output: backend/tests/test_pipeline.py with 5+ test cases covering the full PIPE-01 through PIPE-05 requirements.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-pipeline/06-RESEARCH.md
@.planning/phases/06-pipeline/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test_pipeline.py — RED phase (write tests, confirm they fail without pipeline)</name>
  <files>backend/tests/test_pipeline.py</files>
  <action>
    Create backend/tests/test_pipeline.py. All I/O stages must be patched at the pipeline module namespace (app.ingestion.pipeline.*), not their origin modules — same pattern as test_embedder.py.

    FIXTURE: mock_pipeline_stages(tmp_path)
    Create a pytest fixture that patches all I/O stages simultaneously using unittest.mock.patch as context managers or with patch(...) as ctx:
    - patch("app.ingestion.pipeline.walk_repo", return_value=[{"path": str(tmp_path / "a.py"), "language": "python", "size_kb": 1}])
    - patch("app.ingestion.pipeline.parse_file", return_value=([], []))
    - patch("app.ingestion.pipeline.build_graph", return_value=<nx.DiGraph with 1 node and 1 edge>)
    - patch("app.ingestion.pipeline.save_graph")
    - patch("app.ingestion.pipeline.embed_and_store", return_value=1)
    Build the DiGraph fixture: G = nx.DiGraph(); G.add_node("a.py::func"); G.add_edge("a.py::func", "a.py::func")
    The fixture should use nested `with patch(...)` blocks or `unittest.mock.patch` as decorator stacks, yielding control so tests run inside the patched context.

    TEST CASES (implement all 5):

    1. test_run_ingestion_complete(mock_pipeline_stages, tmp_path)
       - calls: result = asyncio.run(run_ingestion(str(tmp_path), ["python"]))
       - asserts: result.status == "complete", result.nodes_indexed == 1, result.edges_indexed == 1, result.files_processed == 1

    2. test_status_stored_after_run(mock_pipeline_stages, tmp_path)
       - calls run_ingestion, then get_status(str(tmp_path))
       - asserts: status is not None, status.status == "complete"

    3. test_incremental_calls_delete(tmp_path)
       - patches all stages independently (no fixture) — also patches delete_nodes_for_files
       - calls: asyncio.run(run_ingestion(str(tmp_path), ["python"], changed_files=[str(tmp_path / "a.py")]))
       - asserts: delete_nodes_for_files mock was called once with (changed_files_list, str(tmp_path))

    4. test_run_ingestion_error_returns_failed(tmp_path)
       - patches walk_repo with side_effect=RuntimeError("disk error")
       - calls: result = asyncio.run(run_ingestion(str(tmp_path), ["python"]))
       - asserts: result.status == "failed", "disk error" in result.error

    5. test_parse_failure_is_partial_not_fatal(tmp_path)
       - patches walk_repo to return two files
       - patches parse_file with side_effect=[RuntimeError("bad file"), ([], [])] so first call raises, second succeeds
       - asserts: result.status == "complete" (pipeline does not abort on single file failure)

    IMPORTS needed:
    - import asyncio
    - import pytest
    - import networkx as nx
    - from unittest.mock import patch, MagicMock
    - from app.ingestion.pipeline import run_ingestion, get_status
    - from app.models.schemas import IndexStatus

    NOTE: asyncio.to_thread is used internally by the pipeline; since parse_file, save_graph, embed_and_store are patched at the pipeline namespace, the patched synchronous mocks will be passed to asyncio.to_thread and will execute normally in a thread — no special async handling needed in the tests.
  </action>
  <verify>
    Run: cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/test_pipeline.py -v
    Expected: All 5 tests pass (GREEN — pipeline implementation from 06-01 satisfies the contracts)
    Run: cd /Users/mohammedhafiz/Desktop/Personal/nexus/backend && python -m pytest tests/ -q 2>&1 | tail -5
    Expected: All tests pass, count increased by 5 from the prior 57 baseline
  </verify>
  <done>test_pipeline.py exists with 5 passing tests. The full test suite shows 62+ passing tests with 0 failures.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_pipeline.py -v` — all 5 tests pass, each labeled with its requirement behavior
2. `python -m pytest tests/ -q` — full suite passes, total count is 57 + 5 = 62 (or more)
3. All patch targets use "app.ingestion.pipeline.*" namespace (grep confirms no patches targeting "app.ingestion.walker.*" or "app.ingestion.ast_parser.*" directly)
4. No test imports Docker, OpenAI, or psycopg2 — all I/O is mocked
</verification>

<success_criteria>
- test_pipeline.py has at minimum 5 test functions covering: happy path, status polling, incremental delete, error → failed status, partial parse failure
- All 5 tests pass
- Full test suite (62+ tests) passes with 0 failures
- Patch targets are app.ingestion.pipeline.* (not origin modules)
- Tests are self-contained — run without Docker or OpenAI keys
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline/06-03-SUMMARY.md`
</output>
