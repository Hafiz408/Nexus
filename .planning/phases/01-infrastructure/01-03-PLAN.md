---
phase: 01-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "docker compose up starts without errors and postgres reports healthy"
    - "pgvector extension is installed and queryable inside the postgres container"
    - "backend container builds and starts, /health returns 200"
    - ".env is not tracked by git"
    - "data/ directory persists across a container restart cycle"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Running compose stack verified end-to-end"
    - path: ".env"
      provides: "Local secrets file — must exist on disk but NOT in git"
    - path: "data/.gitkeep"
      provides: "Confirms data/ bind mount target exists"
  key_links:
    - from: "postgres container"
      to: "pgvector extension"
      via: "docker exec psql query"
      pattern: "extname.*vector"
    - from: "backend container"
      to: "/health endpoint"
      via: "curl http://localhost:8000/health"
      pattern: "status.*ok"
---

<objective>
Create the local .env file and smoke-test the complete infrastructure stack end-to-end: docker compose up, postgres health check passing, pgvector extension queryable, backend /health responding, and .env confirmed absent from git tracking.

Purpose: Infrastructure is not "done" until it's running. This plan creates the one human-required artifact (.env with real local secrets), starts the stack, and confirms every INFRA requirement is satisfied before Phase 2 begins.
Output: Running local dev environment verified against all 4 INFRA requirements.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env from .env.example and start the stack</name>
  <files>.env</files>
  <action>
Create `.env` at the repo root by copying `.env.example` and substituting safe local development values:

```
POSTGRES_USER=nexus
POSTGRES_PASSWORD=nexus_dev_password
POSTGRES_DB=nexus_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

OPENAI_API_KEY=sk-placeholder
LANGCHAIN_API_KEY=
LANGCHAIN_TRACING_V2=false
LANGCHAIN_PROJECT=nexus-v1
```

Note: OPENAI_API_KEY can be a placeholder for Phase 1 — it is not used until Phase 5 (Embedder). Use an obvious placeholder value like `sk-placeholder` rather than a real key so it's clear the value needs to be updated before Phase 5.

After creating .env, start the stack:
```bash
docker compose up --build -d
```

Wait for the stack to stabilize (postgres health check retries up to 10 times at 5s intervals = 50s max). Check status with:
```bash
docker compose ps
```

Postgres container must show `healthy` status. Backend container must show `Up` status.

If backend fails to start (common first-run issue: postgres not yet accepting connections despite `condition: service_healthy`), check logs:
```bash
docker compose logs backend
```

If there is a connection error, wait 10 more seconds and run `docker compose restart backend`.
  </action>
  <verify>
Run: `docker compose ps` — both `nexus_postgres` and `nexus_backend` must appear.
Check: postgres container status shows `healthy` (not `starting`).
Check: `git check-ignore -v .env` — must return `.gitignore:.env` confirming .env is gitignored.
  </verify>
  <done>
`docker compose ps` shows nexus_postgres as healthy. nexus_backend is Up. `git check-ignore -v .env` confirms .env is gitignored.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete infrastructure stack against all INFRA requirements</name>
  <files></files>
  <action>
Human verifies the running infrastructure stack. Claude has already started the stack in Task 1 (`docker compose up --build -d`). This checkpoint confirms all 4 INFRA requirements are satisfied before Phase 2 begins.

What was built (Plans 01-01 and 01-02):
- docker-compose.yml (postgres + backend, health check, ./data bind mount)
- backend/Dockerfile (python:3.11-slim, psycopg2-binary, --reload)
- backend/requirements.txt (7 packages)
- backend/app/config.py (pydantic-settings BaseSettings, lru_cache)
- backend/app/db/database.py (CREATE EXTENSION IF NOT EXISTS vector, register_vector)
- .env.example (9 documented secrets with placeholder values)
- .gitignore (.env exact match + data/*)
- data/.gitkeep

Run each verification step in order:

1. **INFRA-01 — Postgres health check:**
   ```bash
   docker compose ps
   ```
   Expected: `nexus_postgres` shows `healthy`, `nexus_backend` shows `Up`

2. **INFRA-01 — pgvector extension queryable:**
   ```bash
   docker exec nexus_postgres psql -U nexus -d nexus_db -c "SELECT extname, extversion FROM pg_extension WHERE extname = 'vector';"
   ```
   Expected: One row with `vector` extname and version like `0.8.0`

3. **INFRA-02 — Backend responds:**
   ```bash
   curl http://localhost:8000/health
   ```
   Expected: `{"status":"ok","version":"1.0.0"}`

4. **INFRA-03 — Secrets gitignored:**
   ```bash
   git check-ignore -v .env
   ```
   Expected: `.gitignore:.env`

5. **INFRA-04 — data/ bind mount persists:**
   ```bash
   docker compose restart backend && docker compose ps && ls -la data/
   ```
   Expected: backend comes back Up; data/ exists on host
  </action>
  <verify>
All 5 checks above pass. User types "approved" to continue.
  </verify>
  <done>
All 4 INFRA requirements verified: postgres healthy + pgvector queryable (INFRA-01), backend /health 200 (INFRA-02), .env gitignored + .env.example committed (INFRA-03), data/ persists across restart (INFRA-04).
  </done>
</task>

</tasks>

<verification>
All 4 INFRA requirements verified by the checkpoint:
- INFRA-01: postgres healthy, pgvector extension queryable
- INFRA-02: backend builds from Dockerfile, /health returns 200
- INFRA-03: .env gitignored, .env.example committed with all keys
- INFRA-04: data/ bind mount exists, persists across restart
</verification>

<success_criteria>
All 5 checkpoint verification steps pass. `docker compose ps` shows both services running. pgvector extension query returns one row. curl /health returns {"status":"ok","version":"1.0.0"}. .env is gitignored. data/ directory exists on host.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>
