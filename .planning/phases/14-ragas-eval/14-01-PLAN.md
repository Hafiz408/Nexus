---
phase: 14-ragas-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/golden_qa.json
  - backend/requirements.txt
autonomous: true
requirements:
  - EVAL-01

must_haves:
  truths:
    - "eval/golden_qa.json exists with exactly 30 entries"
    - "All 7 required FastAPI topics are represented (routing x5, dependency_injection x5, middleware x4, background_tasks x4, security x4, request_parsing x4, response_models x4)"
    - "Every entry has id, topic, question, ground_truth, and notes fields"
    - "backend/requirements.txt includes ragas==0.4.3 and pandas"
  artifacts:
    - path: "eval/golden_qa.json"
      provides: "30 Q&A pairs for RAGAS evaluation"
      contains: "Q01"
    - path: "backend/requirements.txt"
      provides: "Updated Python dependencies including ragas and pandas"
      contains: "ragas==0.4.3"
  key_links:
    - from: "eval/run_ragas.py"
      to: "eval/golden_qa.json"
      via: "json.load() at startup"
      pattern: "golden_qa\\.json"
    - from: "eval/run_ragas.py"
      to: "backend/requirements.txt"
      via: "ragas and pandas must be installed"
      pattern: "ragas==0.4.3"
---

<objective>
Create the 30-pair golden Q&A dataset for RAGAS evaluation and add ragas/pandas to backend requirements.

Purpose: RAGAS evaluation needs a static hand-curated ground truth file covering FastAPI internals. Accuracy of `context_precision` metric depends on having a `ground_truth` reference answer per question.
Output: `eval/golden_qa.json` (30 pairs), `backend/requirements.txt` updated.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ragas-eval/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eval/golden_qa.json with 30 FastAPI Q&A pairs</name>
  <files>eval/golden_qa.json</files>
  <action>
Create the directory `eval/` at the repo root (sibling to `backend/` and `extension/`). Create `eval/golden_qa.json` as a JSON array of exactly 30 objects. Each object must have these fields:
- `id`: string, "Q01" through "Q30"
- `topic`: one of the 7 values below
- `question`: natural language question about FastAPI internals
- `ground_truth`: accurate prose answer (2-5 sentences) describing how FastAPI implements the feature; based on official FastAPI documentation; no fabrication
- `notes`: brief annotation (1 line)

Topic distribution (must be exact):
- `routing`: 5 entries (Q01–Q05) — path parameters, HTTP methods, path operations, query params, response status codes
- `dependency_injection`: 5 entries (Q06–Q10) — Depends(), sub-dependencies, class-based dependencies, yield dependencies, dependency overrides
- `middleware`: 4 entries (Q11–Q14) — CORSMiddleware, custom BaseHTTPMiddleware, middleware execution order, adding middleware via add_middleware
- `background_tasks`: 4 entries (Q15–Q18) — BackgroundTasks parameter, add_task, execution timing after response, injecting via Depends
- `security`: 4 entries (Q19–Q22) — OAuth2PasswordBearer, HTTPBasic, API key via Header, scopes
- `request_parsing`: 4 entries (Q23–Q26) — JSON body via Pydantic model, Form fields, File upload, query param validation with Query()
- `response_models`: 4 entries (Q27–Q30) — response_model parameter, response_model_exclude_unset, JSONResponse, custom response class

All ground_truth answers must be factually accurate descriptions of FastAPI behavior. Write them from knowledge of the official FastAPI documentation. Do not fabricate features. If uncertain about an exact detail, describe the general mechanism correctly.

Example entries for guidance:
```json
{
  "id": "Q01",
  "topic": "routing",
  "question": "How does FastAPI define path parameters with type validation?",
  "ground_truth": "FastAPI uses Python type annotations in path operation function signatures. Declaring a parameter like `item_id: int` in both the path string `{item_id}` and the function signature causes FastAPI to automatically parse and validate the value. If validation fails, FastAPI returns a 422 Unprocessable Entity response with details about the error.",
  "notes": "Covers routing + type coercion via Pydantic"
}
```

Write all 30 entries. Ensure valid JSON (no trailing commas, all strings quoted).
  </action>
  <verify>
Run: `python3 -c "import json; d=json.load(open('eval/golden_qa.json')); assert len(d)==30, f'got {len(d)}'; topics=set(e['topic'] for e in d); required={'routing','dependency_injection','middleware','background_tasks','security','request_parsing','response_models'}; assert required==topics, f'missing {required-topics}'; assert all(all(k in e for k in ['id','topic','question','ground_truth','notes']) for e in d), 'missing fields'; print('OK: 30 entries, all topics present, all fields present')"`

Run from the repo root (`/Users/mohammedhafiz/Desktop/Personal/nexus`).
  </verify>
  <done>
`python3 -c "..."` prints `OK: 30 entries, all topics present, all fields present` with no assertion errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ragas==0.4.3 and pandas to backend/requirements.txt</name>
  <files>backend/requirements.txt</files>
  <action>
Read `backend/requirements.txt`. Append the following two lines at the end (after existing dependencies):

```
ragas==0.4.3
pandas
```

Do not change any existing lines. Do not pin pandas to a specific version — ragas 0.4.3 will pull a compatible version as a transitive dependency anyway, and pinning would risk conflict.

Note: `ragas` depends on `datasets` (HuggingFace) which will be installed automatically as a transitive dep. Do not add it explicitly.
  </action>
  <verify>
Run: `grep -E "ragas|pandas" /Users/mohammedhafiz/Desktop/Personal/nexus/backend/requirements.txt`

Expected output includes:
```
ragas==0.4.3
pandas
```
  </verify>
  <done>
Both `ragas==0.4.3` and `pandas` appear in `backend/requirements.txt`. No existing lines were modified.
  </done>
</task>

</tasks>

<verification>
From repo root `/Users/mohammedhafiz/Desktop/Personal/nexus`:

1. `ls eval/golden_qa.json` — file exists
2. `python3 -c "import json; d=json.load(open('eval/golden_qa.json')); print(len(d), 'entries')"` — prints `30 entries`
3. `grep ragas backend/requirements.txt` — prints `ragas==0.4.3`
4. `grep pandas backend/requirements.txt` — prints `pandas`
</verification>

<success_criteria>
- `eval/golden_qa.json` exists with 30 valid Q&A pairs covering all 7 required FastAPI topics
- Every entry has id, topic, question, ground_truth, notes fields
- `backend/requirements.txt` contains `ragas==0.4.3` and `pandas`
- JSON is valid (parseable by `json.load()`)
</success_criteria>

<output>
After completion, create `.planning/phases/14-ragas-eval/14-01-SUMMARY.md`
</output>
