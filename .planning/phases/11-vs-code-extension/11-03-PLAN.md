---
phase: 11-vs-code-extension
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - extension/src/webview/index.tsx
  - extension/src/webview/App.tsx
  - extension/src/webview/index.css
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "React 18 webview renders a chat message list with user and assistant roles"
    - "Token events from window.addEventListener('message') append to the last assistant message in real-time"
    - "Citations rendered as clickable chips that post openFile message to the extension host"
    - "Index status area shows Indexing spinner or Ready with node count or Not indexed state with Index Workspace button"
    - "All CSS uses var(--vscode-*) variables exclusively — no external CSS framework, no hardcoded hex colors"
  artifacts:
    - path: "extension/src/webview/index.tsx"
      provides: "React 18 createRoot entry point mounting App component"
      contains: "createRoot"
    - path: "extension/src/webview/App.tsx"
      provides: "Chat UI with message list, input, citation chips, and status bar"
      contains: "window.addEventListener"
    - path: "extension/src/webview/index.css"
      provides: "CSS using only --vscode-* variables, no external framework"
      contains: "--vscode-"
  key_links:
    - from: "extension/src/webview/App.tsx"
      to: "vscode.postMessage"
      via: "acquireVsCodeApi().postMessage() for query submission and citation clicks"
      pattern: "vscode\\.postMessage"
    - from: "extension/src/webview/App.tsx"
      to: "window.addEventListener"
      via: "message event listener for tokens/citations/done/error/indexStatus from extension host"
      pattern: "addEventListener.*message"
---

<objective>
Build the React 18 webview UI: chat message list with streaming token rendering, citation chips, index status bar, and all styling via VS Code CSS variables.

Purpose: This is the pure rendering layer of the extension. It has no network access and no VS Code API access directly — it only receives data via window.postMessage events and sends user actions back via vscode.postMessage(). Runs parallel to Plan 02 because the two plans touch entirely different files.
Output: Three webview files (index.tsx, App.tsx, index.css) that compile via the webview esbuild target.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-vs-code-extension/11-RESEARCH.md
@.planning/phases/11-vs-code-extension/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webview/index.tsx and webview/index.css</name>
  <files>
    extension/src/webview/index.tsx
    extension/src/webview/index.css
  </files>
  <action>
Create the extension/src/webview/ directory.

**extension/src/webview/index.tsx** — React 18 entry point using createRoot. Uses react-jsx transform (no React import needed with react-jsx, but include it for clarity):

```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';
import './index.css';

const container = document.getElementById('root');
if (container) {
  const root = createRoot(container);
  root.render(<App />);
}
```

**extension/src/webview/index.css** — all styling uses var(--vscode-*) CSS variables exclusively. CHAT-05 prohibits any external CSS framework. VS Code injects all theme colors as CSS variables into webview html element.

```css
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: var(--vscode-font-family);
  font-size: var(--vscode-font-size);
  color: var(--vscode-foreground);
  background: var(--vscode-sideBar-background);
  height: 100vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

#root {
  display: flex;
  flex-direction: column;
  height: 100%;
}

/* Status bar */
.status-bar {
  padding: 6px 10px;
  background: var(--vscode-statusBar-background);
  color: var(--vscode-statusBar-foreground);
  font-size: 11px;
  display: flex;
  align-items: center;
  gap: 8px;
  border-bottom: 1px solid var(--vscode-panel-border);
  flex-shrink: 0;
}

.status-bar button {
  background: var(--vscode-button-background);
  color: var(--vscode-button-foreground);
  border: none;
  padding: 2px 8px;
  border-radius: 2px;
  cursor: pointer;
  font-size: 11px;
}

.status-bar button:hover {
  background: var(--vscode-button-hoverBackground);
}

/* Message list */
.message-list {
  flex: 1;
  overflow-y: auto;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.message {
  padding: 8px 12px;
  border-radius: 4px;
  line-height: 1.5;
  max-width: 100%;
  white-space: pre-wrap;
  word-break: break-word;
}

.message-user {
  background: var(--vscode-input-background);
  color: var(--vscode-input-foreground);
  border: 1px solid var(--vscode-input-border, transparent);
  align-self: flex-end;
  max-width: 85%;
}

.message-assistant {
  background: var(--vscode-editor-background);
  color: var(--vscode-editor-foreground);
  border: 1px solid var(--vscode-panel-border);
  align-self: flex-start;
}

/* Citation chips */
.citations {
  margin-top: 6px;
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
}

.citation-chip {
  display: inline-block;
  background: var(--vscode-badge-background);
  color: var(--vscode-badge-foreground);
  border-radius: 3px;
  padding: 2px 6px;
  font-family: var(--vscode-editor-font-family);
  font-size: calc(var(--vscode-editor-font-size) - 1px);
  cursor: pointer;
  border: none;
  line-height: 1.4;
}

.citation-chip:hover {
  background: var(--vscode-list-hoverBackground);
  color: var(--vscode-list-hoverForeground, var(--vscode-foreground));
}

/* Input area */
.input-area {
  padding: 8px 10px;
  border-top: 1px solid var(--vscode-panel-border);
  display: flex;
  gap: 6px;
  flex-shrink: 0;
}

.input-area textarea {
  flex: 1;
  background: var(--vscode-input-background);
  color: var(--vscode-input-foreground);
  border: 1px solid var(--vscode-input-border, transparent);
  border-radius: 2px;
  padding: 6px 8px;
  font-family: var(--vscode-font-family);
  font-size: var(--vscode-font-size);
  resize: none;
  min-height: 36px;
  max-height: 120px;
}

.input-area textarea:focus {
  outline: 1px solid var(--vscode-focusBorder);
  border-color: var(--vscode-focusBorder);
}

.input-area textarea::placeholder {
  color: var(--vscode-input-placeholderForeground);
}

.input-area button {
  background: var(--vscode-button-background);
  color: var(--vscode-button-foreground);
  border: none;
  border-radius: 2px;
  padding: 6px 12px;
  cursor: pointer;
  font-size: var(--vscode-font-size);
  align-self: flex-end;
}

.input-area button:hover {
  background: var(--vscode-button-hoverBackground);
}

.input-area button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* Spinner */
.spinner {
  display: inline-block;
  width: 10px;
  height: 10px;
  border: 1.5px solid var(--vscode-statusBar-foreground);
  border-top-color: transparent;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```
  </action>
  <verify>
    ls /Users/mohammedhafiz/Desktop/Personal/nexus/extension/src/webview/index.tsx /Users/mohammedhafiz/Desktop/Personal/nexus/extension/src/webview/index.css && echo "Files exist"
  </verify>
  <done>index.tsx uses createRoot() (React 18 API). index.css uses only var(--vscode-*) variables with no hardcoded hex colors or external framework references.</done>
</task>

<task type="auto">
  <name>Task 2: Create webview/App.tsx with full chat UI</name>
  <files>
    extension/src/webview/App.tsx
  </files>
  <action>
**extension/src/webview/App.tsx** — full React chat UI. Key behaviors:
- CHAT-01: Renders message list with user/assistant roles
- CHAT-02: Streaming tokens append to last assistant message in real-time via window.addEventListener('message')
- CHAT-03: Citations render as clickable chips; click sends openFile postMessage to extension host
- CHAT-04: Status bar shows Indexing spinner / "Ready — N nodes" / "Not indexed" + Index Workspace button
- CHAT-05: No external CSS — all styling via index.css which uses --vscode-* only

acquireVsCodeApi() is injected by VS Code into the webview context. Call it ONCE and store the result — calling it multiple times throws an error.

```tsx
import React, { useState, useEffect, useRef, useCallback } from 'react';

// acquireVsCodeApi is injected by VS Code into the webview context.
// Must be called exactly once — store reference.
declare function acquireVsCodeApi(): {
  postMessage(msg: unknown): void;
  getState(): unknown;
  setState(state: unknown): void;
};

interface Citation {
  node_id: string;
  file_path: string;
  line_start: number;
  line_end: number;
  name: string;
  type: string;
}

interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  citations?: Citation[];
  isStreaming?: boolean;
}

interface IndexStatus {
  status: 'pending' | 'running' | 'complete' | 'failed' | 'not_indexed';
  nodes_indexed?: number;
  error?: string | null;
}

type IncomingMessage =
  | { type: 'token'; content: string }
  | { type: 'citations'; citations: Citation[] }
  | { type: 'done'; retrieval_stats: Record<string, unknown> }
  | { type: 'error'; message: string }
  | { type: 'indexStatus'; status: IndexStatus };

// Initialize vscode API once at module level
const vscode = acquireVsCodeApi();

let messageIdCounter = 0;
function newId(): string {
  return String(++messageIdCounter);
}

export function App(): React.JSX.Element {
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [inputValue, setInputValue] = useState('');
  const [isStreaming, setIsStreaming] = useState(false);
  const [indexStatus, setIndexStatus] = useState<IndexStatus>({ status: 'not_indexed' });
  const messagesEndRef = useRef<HTMLDivElement>(null);
  const textareaRef = useRef<HTMLTextAreaElement>(null);

  // Auto-scroll to bottom when messages update
  useEffect(() => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  // CHAT-02: Listen for messages from extension host
  useEffect(() => {
    const handleMessage = (event: MessageEvent): void => {
      const msg = event.data as IncomingMessage;

      switch (msg.type) {
        case 'token':
          // Append token to last assistant message (streaming)
          setMessages((prev) => {
            const last = prev[prev.length - 1];
            if (last && last.role === 'assistant' && last.isStreaming) {
              return [
                ...prev.slice(0, -1),
                { ...last, content: last.content + msg.content },
              ];
            }
            // Start new assistant message
            return [
              ...prev,
              {
                id: newId(),
                role: 'assistant',
                content: msg.content,
                isStreaming: true,
              },
            ];
          });
          break;

        case 'citations':
          // Attach citations to last assistant message
          setMessages((prev) => {
            const last = prev[prev.length - 1];
            if (last && last.role === 'assistant') {
              return [
                ...prev.slice(0, -1),
                { ...last, citations: msg.citations },
              ];
            }
            return prev;
          });
          break;

        case 'done':
          // Mark last assistant message as no longer streaming
          setMessages((prev) => {
            const last = prev[prev.length - 1];
            if (last && last.role === 'assistant') {
              return [
                ...prev.slice(0, -1),
                { ...last, isStreaming: false },
              ];
            }
            return prev;
          });
          setIsStreaming(false);
          break;

        case 'error':
          setMessages((prev) => [
            ...prev,
            {
              id: newId(),
              role: 'assistant',
              content: `Error: ${msg.message}`,
              isStreaming: false,
            },
          ]);
          setIsStreaming(false);
          break;

        case 'indexStatus':
          setIndexStatus(msg.status);
          break;
      }
    };

    window.addEventListener('message', handleMessage);
    return () => window.removeEventListener('message', handleMessage);
  }, []);

  const handleSend = useCallback((): void => {
    const question = inputValue.trim();
    if (!question || isStreaming) {
      return;
    }

    // Add user message immediately
    setMessages((prev) => [
      ...prev,
      { id: newId(), role: 'user', content: question },
    ]);
    setInputValue('');
    setIsStreaming(true);

    // Send query to extension host (which proxies to backend SSE)
    vscode.postMessage({ type: 'query', question });
  }, [inputValue, isStreaming]);

  const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>): void => {
    // Send on Enter, allow Shift+Enter for newline
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  // CHAT-03: Citation click sends openFile to extension host
  const handleCitationClick = (citation: Citation): void => {
    vscode.postMessage({
      type: 'openFile',
      filePath: citation.file_path,
      lineStart: citation.line_start,
    });
  };

  // CHAT-04: Status bar actions
  const handleIndexWorkspace = (): void => {
    vscode.postMessage({ type: 'indexWorkspace' });
  };

  const renderStatusBar = (): React.JSX.Element => {
    const { status, nodes_indexed } = indexStatus;
    let content: React.ReactNode;

    if (status === 'running' || status === 'pending') {
      content = (
        <>
          <span className="spinner" />
          <span>Indexing{nodes_indexed !== undefined ? ` — ${nodes_indexed} nodes` : '...'}</span>
        </>
      );
    } else if (status === 'complete' && nodes_indexed !== undefined) {
      content = <span>Ready — {nodes_indexed} nodes</span>;
    } else if (status === 'failed') {
      content = (
        <>
          <span>Index failed</span>
          <button onClick={handleIndexWorkspace}>Retry</button>
        </>
      );
    } else {
      content = (
        <>
          <span>Not indexed</span>
          <button onClick={handleIndexWorkspace}>Index Workspace</button>
        </>
      );
    }

    return <div className="status-bar">{content}</div>;
  };

  return (
    <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
      {/* CHAT-04: Index status bar */}
      {renderStatusBar()}

      {/* CHAT-01: Message list */}
      <div className="message-list">
        {messages.map((msg) => (
          <div
            key={msg.id}
            className={`message ${msg.role === 'user' ? 'message-user' : 'message-assistant'}`}
          >
            <div>{msg.content}</div>
            {/* CHAT-03: Citation chips */}
            {msg.citations && msg.citations.length > 0 && (
              <div className="citations">
                {msg.citations.map((c) => (
                  <button
                    key={c.node_id}
                    className="citation-chip"
                    onClick={() => handleCitationClick(c)}
                    title={`${c.file_path}:${c.line_start}-${c.line_end}`}
                  >
                    {c.file_path.split('/').pop()}:{c.line_start}
                  </button>
                ))}
              </div>
            )}
          </div>
        ))}
        <div ref={messagesEndRef} />
      </div>

      {/* Input area */}
      <div className="input-area">
        <textarea
          ref={textareaRef}
          value={inputValue}
          onChange={(e) => setInputValue(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="Ask about your codebase..."
          disabled={isStreaming}
          rows={1}
        />
        <button onClick={handleSend} disabled={isStreaming || !inputValue.trim()}>
          {isStreaming ? '...' : 'Ask'}
        </button>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    cd /Users/mohammedhafiz/Desktop/Personal/nexus/extension && npx tsc --noEmit -p tsconfig.webview.json 2>&1 | head -30 || echo "Check output above"
  </verify>
  <done>App.tsx compiles with 0 TypeScript errors under tsconfig.webview.json. App has: message list with user/assistant roles (CHAT-01), streaming token append (CHAT-02), citation chips with openFile postMessage (CHAT-03), status bar with all three states (CHAT-04), and all styling via index.css --vscode-* variables only (CHAT-05).</done>
</task>

</tasks>

<verification>
1. extension/src/webview/index.tsx uses createRoot() from react-dom/client (React 18 API, not legacy ReactDOM.render)
2. extension/src/webview/App.tsx: acquireVsCodeApi() called once at module level, stored as const vscode
3. App.tsx window.addEventListener('message') handles token/citations/done/error/indexStatus event types
4. Token event appends to last streaming assistant message (not creates new message each time)
5. Citation chips call vscode.postMessage({ type: 'openFile', filePath, lineStart }) on click
6. Status bar shows spinner for pending/running, "Ready — N nodes" for complete, "Not indexed" + button otherwise
7. index.css contains zero hardcoded hex colors — only var(--vscode-*) variables
8. tsc --noEmit -p tsconfig.webview.json exits with 0 errors
</verification>

<success_criteria>
- All three webview files exist and TypeScript compiles clean
- App.tsx acquireVsCodeApi() called exactly once (module-level, not in component)
- Streaming works: token events append to last assistant message using functional state update
- Citations render as chips that post openFile to extension host
- Status bar covers all four states: not_indexed, pending/running, complete, failed
- CSS exclusively uses VS Code CSS variables (grep for 'px\|rem\|em' in color properties would find none)
</success_criteria>

<output>
After completion, create `.planning/phases/11-vs-code-extension/11-03-SUMMARY.md`
</output>
