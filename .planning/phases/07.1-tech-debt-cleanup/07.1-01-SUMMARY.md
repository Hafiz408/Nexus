---
phase: 07.1-tech-debt-cleanup
plan: 01
subsystem: database
tags: [sqlite, fts5, pgvector, embedder, pipeline, incremental-index]

# Dependency graph
requires:
  - phase: 05-embedder
    provides: embed_and_store, code_fts, code_embeddings tables, FTS5 upsert pattern
  - phase: 06-pipeline
    provides: run_ingestion incremental path, delete_nodes_for_files wiring
provides:
  - delete_embeddings_for_files(file_paths, repo_path) in embedder.py
  - incremental re-index path now cleans both graph_nodes and code_fts/code_embeddings
  - tests: empty-list no-op, FTS5 row deletion, pipeline incremental assertion
affects: [08-query-endpoint, any phase consuming FTS5 or pgvector search results]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - file_path-scoped deletion for FTS5 (plain WHERE, not MATCH, because file_path is UNINDEXED)
    - symmetric delete pair in incremental path: delete_nodes_for_files + delete_embeddings_for_files

key-files:
  created: []
  modified:
    - backend/app/ingestion/embedder.py
    - backend/app/ingestion/pipeline.py
    - backend/tests/test_embedder.py
    - backend/tests/test_pipeline.py

key-decisions:
  - "Plain WHERE file_path IN (...) for FTS5 delete — MATCH syntax invalid on UNINDEXED columns"
  - "delete_embeddings_for_files placed immediately after delete_embeddings_for_repo — symmetric API, same connection pattern"
  - "Empty file_paths guard returns before any DB connection opened — no-op is safe and cheap"

patterns-established:
  - "Symmetric delete functions: delete_X_for_repo / delete_X_for_files — full-repo vs incremental scopes"
  - "Incremental path always calls both graph-store and embedder deletes before re-parsing"

requirements-completed: [EMBED-05, PIPE-03, STORE-03]

# Metrics
duration: 2min
completed: 2026-03-19
---

# Phase 07.1 Plan 01: Tech Debt Cleanup Summary

**File-scoped FTS5 and pgvector stale-row deletion via delete_embeddings_for_files, closing the incremental re-index data-consistency gap for renamed/removed functions**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-19T03:02:37Z
- **Completed:** 2026-03-19T03:04:57Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `delete_embeddings_for_files(file_paths, repo_path)` to `embedder.py` — deletes from both `code_embeddings` (pgvector) and `code_fts` (FTS5) by file_path, with empty-list guard
- Wired new function into `pipeline.py` incremental path alongside existing `delete_nodes_for_files` — stale rows no longer persist in search stores after rename/delete
- Three new tests cover empty-list no-op, FTS5 row deletion, and pipeline incremental call assertion; all 65 tests pass with no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement delete_embeddings_for_files in embedder.py** - `0121728` (feat)
2. **Task 2: Wire into pipeline.py + add tests** - `89caa49` (feat)

## Files Created/Modified
- `backend/app/ingestion/embedder.py` - Added `delete_embeddings_for_files` after `delete_embeddings_for_repo`
- `backend/app/ingestion/pipeline.py` - Updated import and added call in incremental path
- `backend/tests/test_embedder.py` - Added two new tests for `delete_embeddings_for_files`
- `backend/tests/test_pipeline.py` - Added `test_incremental_calls_delete_embeddings`

## Decisions Made
- Used plain `WHERE file_path IN (...)` for FTS5 (not MATCH) because `file_path` is declared `UNINDEXED` — MATCH on UNINDEXED columns is invalid in FTS5
- delete_embeddings_for_files uses the same try/finally connection-close pattern as delete_embeddings_for_repo for consistency
- Empty list guard returns before any DB connection is opened — zero cost for no-op case

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Incremental re-index now consistently cleans all three stores (graph_nodes/graph_edges, code_embeddings, code_fts)
- Phase 07.1 plan 01 complete; subsequent tech-debt plans can proceed
- No blockers or concerns

---
*Phase: 07.1-tech-debt-cleanup*
*Completed: 2026-03-19*
