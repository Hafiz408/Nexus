---
phase: 07.1-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ingestion/embedder.py
  - backend/app/ingestion/pipeline.py
  - backend/tests/test_embedder.py
  - backend/tests/test_pipeline.py
autonomous: true
requirements:
  - EMBED-05
  - PIPE-03
  - STORE-03

must_haves:
  truths:
    - "Incremental re-index with changed_files removes stale FTS5 rows for renamed/removed functions"
    - "Incremental re-index with changed_files removes stale pgvector rows for the targeted files"
    - "An empty changed_files list is a safe no-op — no errors, no deletes"
    - "Existing pipeline tests still pass after the wiring change"
  artifacts:
    - path: "backend/app/ingestion/embedder.py"
      provides: "delete_embeddings_for_files function"
      contains: "def delete_embeddings_for_files"
    - path: "backend/app/ingestion/pipeline.py"
      provides: "incremental path that calls both delete functions"
      contains: "delete_embeddings_for_files"
    - path: "backend/tests/test_embedder.py"
      provides: "tests for delete_embeddings_for_files"
      contains: "test_delete_embeddings_for_files"
    - path: "backend/tests/test_pipeline.py"
      provides: "test for incremental path calling both deletes"
      contains: "test_incremental_calls_delete_embeddings"
  key_links:
    - from: "backend/app/ingestion/pipeline.py"
      to: "backend/app/ingestion/embedder.py"
      via: "import delete_embeddings_for_files"
      pattern: "from app\\.ingestion\\.embedder import.*delete_embeddings_for_files"
    - from: "pipeline.py incremental path"
      to: "delete_embeddings_for_files()"
      via: "called alongside delete_nodes_for_files when changed_files is not None"
      pattern: "delete_embeddings_for_files\\(changed_files"
---

<objective>
Add `delete_embeddings_for_files(file_paths, repo_path)` to `embedder.py`, wire it into the incremental re-index path in `pipeline.py`, and test both additions.

Purpose: Close the data-consistency gap where incremental re-index (`changed_files`) correctly cleans `graph_nodes`/`graph_edges` (via `delete_nodes_for_files`) but leaves stale rows in `code_fts` (FTS5) and `code_embeddings` (pgvector). Stale FTS5 rows cause renamed/removed functions to persist in name-search results indefinitely.

Output: `delete_embeddings_for_files` in `embedder.py`, one-line wiring in `pipeline.py`, tests in `test_embedder.py` and `test_pipeline.py`.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-tech-debt-cleanup/07.1-RESEARCH.md
@backend/app/ingestion/embedder.py
@backend/app/ingestion/pipeline.py
@backend/tests/test_embedder.py
@backend/tests/test_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement delete_embeddings_for_files in embedder.py</name>
  <files>backend/app/ingestion/embedder.py</files>
  <action>
Add `delete_embeddings_for_files(file_paths: list[str], repo_path: str) -> None` to `embedder.py`.

Place it immediately after `delete_embeddings_for_repo` (which is the existing full-repo deletion function). Follow the same connection/cursor pattern that function uses.

The function must:
1. Guard against empty list: `if not file_paths: return`
2. Delete from pgvector `code_embeddings` table — use `get_db_connection()` + `register_vector(pg_conn)`, then: `DELETE FROM code_embeddings WHERE repo_path = %s AND file_path IN ({placeholders})` where placeholders = `", ".join(["%s"] * len(file_paths))` and params = `[repo_path, *file_paths]`. Close the connection in a `finally` block.
3. Delete from FTS5 `code_fts` table — use `sqlite3.connect(_sqlite_db_path())`, then: `DELETE FROM code_fts WHERE file_path IN ({placeholders})` where placeholders = `", ".join(["?"] * len(file_paths))`. Call `sqlite_conn.commit()`. Close the connection in a `finally` block.

CRITICAL: `file_path` in `code_fts` is declared `UNINDEXED` — do NOT use FTS5 `MATCH` syntax. Use plain `WHERE file_path IN (...)` SQL.
CRITICAL: Use `WHERE file_path IN (...)` not `WHERE node_id IN (...)` — the deletion must be scoped by file, not individual node.

Do not touch any other functions in the file.
  </action>
  <verify>
Run from the repo root:
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus
grep -n "def delete_embeddings_for_files" backend/app/ingestion/embedder.py
grep -n "code_fts" backend/app/ingestion/embedder.py
grep -n "file_path IN" backend/app/ingestion/embedder.py
```
All three should return results. Then run `python -c "from backend.app.ingestion.embedder import delete_embeddings_for_files; print('OK')"` or equivalent import check.
  </verify>
  <done>
`delete_embeddings_for_files` exists in `embedder.py`, deletes from both `code_embeddings` (pgvector) and `code_fts` (FTS5) by `file_path IN (...)`, guards against empty list, closes connections in `finally` blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire delete_embeddings_for_files into pipeline.py incremental path + add tests</name>
  <files>backend/app/ingestion/pipeline.py, backend/tests/test_embedder.py, backend/tests/test_pipeline.py</files>
  <action>
**pipeline.py changes:**

1. Update the import from `embedder` at the top of the file. Current import: `from app.ingestion.embedder import embed_and_store`. New import: `from app.ingestion.embedder import embed_and_store, delete_embeddings_for_files`

2. Find the incremental path block (where `changed_files is not None` is checked). It currently calls only `delete_nodes_for_files(changed_files, repo_path)`. Add `delete_embeddings_for_files(changed_files, repo_path)` immediately after on the next line. No other changes to `pipeline.py`.

**test_embedder.py additions:**

Add two new test functions at the end of the file:

Test 1 — `test_delete_embeddings_for_files_empty_list_is_noop`:
- Call `delete_embeddings_for_files([], "/tmp/repo")` — must not raise, must not call any DB connection
- Verify with: mock `get_db_connection` and confirm it is NOT called when file_paths is empty

Test 2 — `test_delete_embeddings_for_files_removes_fts5_rows`:
- Use the existing `tmp_db` fixture (SQLite temp DB) and `sample_nodes` fixture
- Call `embed_and_store(sample_nodes, "/tmp/test_repo")` with appropriate patches for `get_db_connection`, `register_vector`, `execute_values`, and `OpenAI` (match the patch namespace `app.ingestion.embedder.*` as used in existing tests)
- After embed, verify FTS5 rows exist: `sqlite3.connect(tmp_db_path)` → `SELECT COUNT(*) FROM code_fts` should be > 0
- Call `delete_embeddings_for_files([sample_nodes[0].file_path], "/tmp/test_repo")` with `get_db_connection` and `register_vector` patched (pgvector side)
- Verify FTS5 rows for that file_path are gone: `SELECT COUNT(*) FROM code_fts WHERE file_path = ?` should return 0

**test_pipeline.py additions:**

Add one new test function: `test_incremental_calls_delete_embeddings`:
- Create a minimal `tmp_path` with one `.py` file
- `changed = [str(tmp_path / "a.py")]`
- Patch `app.ingestion.pipeline.delete_nodes_for_files` as `mock_graph_del`
- Patch `app.ingestion.pipeline.delete_embeddings_for_files` as `mock_emb_del`
- Patch `app.ingestion.pipeline.walk_repo` to return `[{"path": changed[0], "language": "python", "size_kb": 1}]`
- Patch `app.ingestion.pipeline.parse_file` to return `([], [])`
- Patch `app.ingestion.pipeline.build_graph` to return `nx.DiGraph()`
- Patch `app.ingestion.pipeline.save_graph`
- Patch `app.ingestion.pipeline.embed_and_store` to return `0`
- Call `asyncio.run(run_ingestion(str(tmp_path), ["python"], changed_files=changed))`
- Assert `mock_graph_del.assert_called_once_with(changed, str(tmp_path))`
- Assert `mock_emb_del.assert_called_once_with(changed, str(tmp_path))`

Use `asyncio.run()` (not pytest-asyncio) to match the existing pipeline test pattern in `test_pipeline.py`.
  </action>
  <verify>
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus
grep -n "delete_embeddings_for_files" backend/app/ingestion/pipeline.py
python -m pytest backend/tests/test_pipeline.py -v 2>&1 | tail -20
python -m pytest backend/tests/test_embedder.py -v 2>&1 | tail -20
```
All existing tests must still pass. New tests must appear and pass.
  </verify>
  <done>
`pipeline.py` imports `delete_embeddings_for_files` and calls it in the incremental path. All existing tests pass. Three new tests pass: empty-list no-op, FTS5 row deletion, and pipeline incremental path assertion.
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus
python -m pytest backend/tests/ -v 2>&1 | tail -30
```

Verify the key wiring exists:
```
grep -n "delete_embeddings_for_files" backend/app/ingestion/pipeline.py backend/app/ingestion/embedder.py
```

Expected: at minimum 3 lines — 1 in pipeline.py import, 1 in pipeline.py call, 2+ in embedder.py (def + body).
</verification>

<success_criteria>
1. `delete_embeddings_for_files(file_paths, repo_path)` exists in `embedder.py` and deletes from both `code_fts` (FTS5) and `code_embeddings` (pgvector) by `file_path IN (...)`
2. `pipeline.py` incremental path calls both `delete_nodes_for_files` and `delete_embeddings_for_files` when `changed_files is not None`
3. Empty `file_paths` list is a no-op (no DB calls, no errors)
4. All existing tests pass — no regressions
5. New tests in `test_embedder.py` and `test_pipeline.py` pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-tech-debt-cleanup/07.1-01-SUMMARY.md`
</output>
