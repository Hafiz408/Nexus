---
phase: 07.1-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "The backend service in docker-compose.yml has a healthcheck stanza"
    - "The healthcheck uses GET /health which already exists in main.py"
    - "The backend service reports healthy after uvicorn startup completes"
  artifacts:
    - path: "docker-compose.yml"
      provides: "backend service healthcheck"
      contains: "healthcheck"
  key_links:
    - from: "docker-compose.yml backend.healthcheck"
      to: "GET /health endpoint in main.py"
      via: "curl -f http://localhost:8000/health"
      pattern: "curl.*localhost:8000/health"
---

<objective>
Add a Docker Compose `healthcheck` stanza to the `backend` service in `docker-compose.yml` using the existing `GET /health` endpoint.

Purpose: The `postgres` service already has a healthcheck; the `backend` service does not. Adding one enables `depends_on: condition: service_healthy` for future services and makes container orchestration reliable.

Output: `docker-compose.yml` with `backend.healthcheck` using `curl -f http://localhost:8000/health`, interval 10s, timeout 5s, retries 5, start_period 15s.
</objective>

<execution_context>
@/Users/mohammedhafiz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohammedhafiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-tech-debt-cleanup/07.1-RESEARCH.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add healthcheck stanza to backend service in docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Read the current `docker-compose.yml`. Locate the `backend` service definition. Add a `healthcheck` stanza to the `backend` service using the `CMD` array format (not CMD-SHELL):

```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 15s
```

Place the `healthcheck` block within the `backend` service definition, at the same indentation level as `build`, `ports`, `volumes`, `depends_on`, and `env_file`. Follow the exact same YAML key ordering and indentation style used by the `postgres` service's existing `healthcheck` stanza.

`start_period: 15s` rationale: uvicorn + pgvector DB init can take 2-8s; 15s gives headroom without being excessive. The postgres service uses `start_period: 10s`; backend needs slightly more since it waits for postgres first.

Do NOT use CMD-SHELL format. Do NOT use wget. Do NOT add `curl` to the Dockerfile — `python:3.11-slim` includes curl by default.

Do not modify any other part of the file.
  </action>
  <verify>
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus
grep -A 6 "healthcheck" docker-compose.yml
```
Should show at least two healthcheck blocks (postgres + backend). Confirm the backend block contains `curl` and `localhost:8000/health`.

Validate YAML syntax:
```
python3 -c "import yaml; yaml.safe_load(open('docker-compose.yml'))" && echo "YAML valid"
```
  </verify>
  <done>
`docker-compose.yml` contains a `healthcheck` stanza under the `backend` service using `["CMD", "curl", "-f", "http://localhost:8000/health"]` with interval 10s, timeout 5s, retries 5, start_period 15s. YAML is valid (no parse errors).
  </done>
</task>

</tasks>

<verification>
Confirm both services now have healthchecks:
```
cd /Users/mohammedhafiz/Desktop/Personal/nexus
grep -n "healthcheck" docker-compose.yml
python3 -c "import yaml; data=yaml.safe_load(open('docker-compose.yml')); hc=data['services']['backend']['healthcheck']; print('test:', hc['test']); print('start_period:', hc.get('start_period'))"
```

Expected output: backend healthcheck test = `["CMD", "curl", "-f", "http://localhost:8000/health"]` and start_period = `15s`.
</verification>

<success_criteria>
1. `docker-compose.yml` backend service has a `healthcheck` stanza
2. Healthcheck uses `["CMD", "curl", "-f", "http://localhost:8000/health"]`
3. `start_period: 15s` is set to allow uvicorn startup time
4. YAML parses without errors
5. No other changes to the file
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-tech-debt-cleanup/07.1-02-SUMMARY.md`
</output>
